@model IEnumerable<The_Someren.Models.Student>

@{
    ViewData["Title"] = "Index";
}

<h1>GetAllStudent</h1>

<p>
    <a asp-controller="Student" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>
                    @Html.DisplayFor(model => item.StudentID)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Name)
                </th>
                <th>
                    @Html.DisplayFor(model => item.StudentNumber)
                </th>
                <th>
                    @Html.DisplayFor(model => item.PhoneNumber)
                </th>
                <th>
                    @Html.DisplayFor(model => item.Class)
                </th>
                             <td>
                    @Html.ActionLink("Update", "Update", new { id = item.StudentID }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.StudentID  })
            </td> 
            </tr>
        }
    </tbody>
</table>
